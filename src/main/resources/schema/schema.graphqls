type Query {
    orders(orderId: String) : [Order]
}

type Order {
    id: ID!
    items: [Item!]!
    createdAt: String!
}

type Item {
    id: ID!
    name: String!
    desc: String!
    createdAt: DateTime
}

scalar DateTime
